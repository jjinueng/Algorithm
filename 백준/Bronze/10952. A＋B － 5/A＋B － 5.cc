#include <bits/stdc++.h>
#define fastio cin.tie(0)->sync_with_stdio(0)
#define ll long long
#define endl '\n'
using namespace std;

int main() {
    int a;
    int b;
    while(1){
        cin >> a;
        cin >> b;
        if(a==0&&b==0)
            break;
        cout << a+b << endl;
    }

    return 0;
}
