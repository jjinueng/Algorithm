#include <bits/stdc++.h>

#define fastio cin.tie(0)->sync_with_stdio(0);
#define ll long long
#define endl '\n'

using namespace std;

int main() {
    fastio;
    int n;
    cin >> n;
    if(n==0) cout << "YONSEI";
    else if(n==1) cout << "Leading the Way to the Future";
    return 0;
}