#include <bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false), cin.tie(nullptr);
    int n;
    cin >> n;
    if (n % 2 == 0) cout << "Duck";
    else cout << "Goose";
    return 0;
}
